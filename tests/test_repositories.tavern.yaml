---
test_name: List repositories

stages:
  - name: List repositories

    request:
      url: http://localhost/api/v1/repositories
      method: GET

    response:
      status_code: 200
      body: []
